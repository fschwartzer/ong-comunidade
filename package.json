{
  "name": "ong-comunidade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Projeto acadêmico: SPA acessível de ONG fictícia",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "build": "vite build && node scripts/minificar-html.js",
    "preview": "vite preview --host 127.0.0.1",
    "medir": "node scripts/medir-build.js"
  },
  "devDependencies": {
    "html-minifier-terser": "7.2.0",
    "jsdom": "29.1.1",
    "vite": "8.3.0"
  }
}
